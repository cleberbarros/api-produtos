spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gerenciadorprodutos
    username: postgres
    password: 123456
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
server:
  servlet:
    contextPath: /api-produtos/
  port: 8080